import 'package:flutter/material.dart';

class AppStyles {
    static const splashtextstyle = TextStyle(fontSize: 22,fontWeight:FontWeight.bold,color: Colors.white);
    static const appbartextstyle = TextStyle(fontSize: 22,fontWeight:FontWeight.bold,color: Colors.white);
}